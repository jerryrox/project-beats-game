namespace PBGame.UI.Models.MenuBar
{
    /// <summary>
    /// The type of inner menus available through the menu bar.
    /// </summary>
    public enum MenuType {
    
        None = 0,
        Quick,
        Profile,
        Mode,
        Music,
        Settings,
        Notification
    }
}