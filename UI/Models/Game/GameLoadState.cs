namespace PBGame.UI.Models.Game
{
    /// <summary>
    /// The types of states when loading the game.
    /// </summary>
    public enum GameLoadState {
    
        Idle,
        Loading,
        Success,
        Fail
    }
}