namespace PBGame.Audio
{
    /// <summary>
    /// The types of beat frequency scale.
    /// </summary>
    public enum BeatFrequency {
    
        Full = 1,
        Half = 2,
        Quarter = 4,
    }
}